.env
# Claude Code: チーム共有設定 (.claude/settings.json) と規約 (.claude/CLAUDE.md) はコミットする。
# 個人用ローカル設定・リポ直下の個人メモ用 CLAUDE.md だけ除外する。
.claude/settings.local.json
/CLAUDE.md

infrastructure/

frestyle-infrastructure/

# ステージング環境の管理用プライベートリポ（FreStyle 配下に clone する運用、本リポの管理対象外）
frestyle-staging/

# PdM 用プライベートリポ（FreStyle 配下に clone する運用、本リポの管理対象外）
frestyle-pdm/

# 教材コンテンツ（Markdown 原本）の管理用プライベートリポ（FreStyle 配下に clone する運用、本リポの管理対象外）
frestyle-teaching-materials/

# Supabase CLI のローカルリンク状態（pooler-url 等を含むため追跡しない）
supabase/

# IntelliJ IDEA / GoLand
.idea/

# macOS
.DS_Store
**/.DS_Store

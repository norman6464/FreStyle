// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: page_view.sql

package sqlcgen

import (
	"context"
	"encoding/json"
	"time"

	"github.com/google/uuid"
)

const countPageViews = `-- name: CountPageViews :one
SELECT count(*) FROM page_views WHERE page_id = $1
`

// そのページの閲覧数。upsert 型の表なので「延べ回数」ではなく「見たことのある人数」になる
// （page_views.sql の表コメント参照）。
func (q *Queries) CountPageViews(ctx context.Context, pageID uuid.UUID) (int64, error) {
	row := q.db.QueryRowContext(ctx, countPageViews, pageID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const listRecentPageViewCandidates = `-- name: ListRecentPageViewCandidates :many
SELECT
  v.page_id,
  v.viewed_at,
  p.workspace_id,
  p.title,
  p.icon,
  p.space_id,
  s.name AS space_name,
  w.slug AS workspace_slug
FROM page_views v
JOIN pages p ON p.workspace_id = v.workspace_id AND p.id = v.page_id
JOIN spaces s ON s.workspace_id = p.workspace_id AND s.id = p.space_id
JOIN workspaces w ON w.id = p.workspace_id
WHERE v.user_id = $1 AND p.archived_at IS NULL
ORDER BY v.viewed_at DESC
LIMIT 30
`

type ListRecentPageViewCandidatesRow struct {
	PageID        uuid.UUID
	ViewedAt      time.Time
	WorkspaceID   uuid.UUID
	Title         string
	Icon          *json.RawMessage
	SpaceID       uuid.UUID
	SpaceName     string
	WorkspaceSlug string
}

// 自分の「最近見たページ」候補を viewed_at の新しい順に返す。可視判定はここでは行わない
// （呼び出し元の ListMyRecentPagesUseCase が CheckPagePermissionUseCase を通してからふるう。
// SearchViewablePagesUseCase と同じ考え方 — SQL 側で件数を絞ると、ふるいで削られる分だけ
// 本来見えるはずの行を取りこぼす）。アーカイブ済みのページは候補にしない。
func (q *Queries) ListRecentPageViewCandidates(ctx context.Context, userID int64) ([]ListRecentPageViewCandidatesRow, error) {
	rows, err := q.db.QueryContext(ctx, listRecentPageViewCandidates, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRecentPageViewCandidatesRow{}
	for rows.Next() {
		var i ListRecentPageViewCandidatesRow
		if err := rows.Scan(
			&i.PageID,
			&i.ViewedAt,
			&i.WorkspaceID,
			&i.Title,
			&i.Icon,
			&i.SpaceID,
			&i.SpaceName,
			&i.WorkspaceSlug,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertPageView = `-- name: UpsertPageView :exec
INSERT INTO page_views (user_id, workspace_id, page_id, viewed_at)
VALUES ($1, $2, $3, now())
ON CONFLICT (user_id, page_id) DO UPDATE SET viewed_at = now()
WHERE page_views.workspace_id = EXCLUDED.workspace_id
`

type UpsertPageViewParams struct {
	UserID      int64
	WorkspaceID uuid.UUID
	PageID      uuid.UUID
}

// 人 × ページの「最後に見た日時」を 1 行だけ持つ（開くたびに upsert。来訪ごとに行を積まない）。
// 衝突キー (user_id, page_id) は両方とも所有者列だが、workspace_id もこの表に乗っている
// 所有者列のため、DO UPDATE の WHERE で workspace_id も絞る（queries_static_check_test.go が
// 求める多層防御。blocks の UpsertBlock と同じ形。knowledge_base.sql 冒頭のコメント参照）。
func (q *Queries) UpsertPageView(ctx context.Context, arg UpsertPageViewParams) error {
	_, err := q.db.ExecContext(ctx, upsertPageView, arg.UserID, arg.WorkspaceID, arg.PageID)
	return err
}

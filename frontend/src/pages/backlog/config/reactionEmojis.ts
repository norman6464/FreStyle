/**
 * 発言への反応で選べる絵文字。厳選 12 種の固定で、自由入力は付けない。
 *
 * backend の反応 1 件の上限は UTF-8 で 32 **バイト**（文字数ではない）で、許可リストは
 * backend 側に無い。単純な絵文字（連結記号を含まない）は数バイトで収まるが、
 * 家族・職業などの合成絵文字（ゼロ幅結合子で複数の絵文字を繋いだもの）は
 * 簡単に上限を超えて保存に失敗する。共有される列（他人にも見える）なので、
 * 「入れられる絵文字と入れられない絵文字がある」という説明しにくい失敗を
 * 画面側の選択肢を絞ることで消す。
 */
export const REACTION_EMOJIS: readonly string[] = [
  '👍', '🙏', '🎉', '👀', '✅', '❤️', '😄', '🤔', '🚀', '🔥', '⚠️', '😢',
];
